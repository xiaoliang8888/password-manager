# 数据库连接 URL
# 格式: postgresql://用户名:密码@主机:端口/数据库名
DATABASE_URL="postgresql://user:password@localhost:5432/password_manager"

# NextAuth 配置
# NEXTAUTH_SECRET 是用于加密会话的密钥，必须是一个随机字符串
# 你可以使用以下命令生成: openssl rand -base64 32
NEXTAUTH_SECRET="your-secret-key-here-please-change-this"

# NEXTAUTH_URL 是你的应用的完整 URL
# 在开发环境中通常是 http://localhost:3000
# 在生产环境中应该是你的域名，例如 https://yourdomain.com
NEXTAUTH_URL="http://localhost:3000"
